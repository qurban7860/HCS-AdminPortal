import React from 'react';
import { IconButton } from '@mui/material';
import { useTheme, styled, alpha } from '@mui/material/styles';

function ThumbnailIconButtons() {
  const theme = useTheme();

  return <></>;
}

export default ThumbnailIconButtons;
